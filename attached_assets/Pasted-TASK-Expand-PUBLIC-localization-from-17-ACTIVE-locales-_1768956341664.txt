TASK: Expand PUBLIC localization from 17 ACTIVE locales to FULL 30 locales (no content translation yet) — BUILD MODE

CONTEXT:
We already have i18next resources for ~30 locales and want the site to be “localization-ready” for 30 languages.
Right now the public infrastructure (sitemaps/hreflang/locale validation) still uses 17 locales in one or more places (e.g., ACTIVE_LOCALES / localeEnum / SUPPORTED_LOCALES / sitemap service).

GOAL:
Make the public system treat ALL 30 locales as supported & active, end-to-end:
- URL prefix recognition
- SSR/route validation
- hreflang generation
- sitemap index + per-locale sitemaps
- Content-Language header
- locale lists in UI selectors (where applicable)

STRICT REQUIREMENTS:
1) Do NOT add automatic translation. Do NOT generate translated content.
2) This is infra-only: enabling 30 locales for routing/SEO/sitemaps/headers.
3) Keep fallback behavior: requested locale → en → null (or en content), unchanged.
4) Ensure Google-facing sitemaps use ONLY https://travi.world (never dev/replit domains).
5) No regressions for existing 17 locales.

WORK TO DO (CONCRETE):

A) Find the “active locales” source of truth
- Search for: ACTIVE_LOCALES, SUPPORTED_LOCALES, localeEnum, supportedLocales, RTL_LOCALES, i18n resources list
- Identify every place that hardcodes 17 locales or a 17-locale enum.

B) Unify to 30 locales
- Set a single canonical list used by:
  1) server middleware that parses URL locale prefixes
  2) SSR middleware validation (reject/redirect invalid locale prefixes)
  3) sitemap service generation
  4) hreflang tags generation (x-default included)
  5) any “language selector” options shown to users

C) Sitemap updates
- /sitemap.xml index must include 30 locale sitemaps:
  /sitemap-en.xml, /sitemap-ar.xml, ... (30 total)
- Each locale sitemap must list URLs with that locale prefix, except if you intentionally keep “en unprefixed”.
  If EN is unprefixed, still include en sitemap entries without /en prefix and keep alternates via hreflang.
- Validate domain is travi.world for every <loc>.
- If you include hreflang alternates inside sitemaps, ensure alternates cover all 30 locales (or the full supported set) + x-default.

D) hreflang updates
- Ensure SeoHead (and any server-side HTML) outputs 30 alternates + x-default.
- Canonical rules must remain consistent (don’t change strategy unless already defined).

E) Headers / caching
- Content-Language must reflect the locale for ALL 30 locale-prefixed routes.
- Vary: Accept-Language should remain (or adjust if you vary by path only).
- No Cloudflare-specific changes in this task unless absolutely required.

F) Tests / proof
Provide proofs:
1) Print the final 30-locale array (exact values).
2) curl -I:
   - / -> Content-Language: en
   - /ar/ -> Content-Language: ar
   - /pt/ (or another “non-17” locale) -> Content-Language: pt
3) /sitemap.xml includes 30 sitemap links.
4) Open one “new locale” sitemap (e.g., /sitemap-pt.xml) and confirm:
   - All <loc> are travi.world
   - URLs are in the expected locale form
5) View-source of /pt/ (or /pt/destinations) includes hreflang for 30 locales.

DELIVERABLE:
- Public locale system upgraded from 17 → 30 everywhere (routing, SEO, sitemaps, headers).
- Zero automatic translation.
- No regressions.

START NOW.