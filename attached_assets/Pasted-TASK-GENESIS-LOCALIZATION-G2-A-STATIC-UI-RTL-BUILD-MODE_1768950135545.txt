TASK: GENESIS LOCALIZATION — G2-A (STATIC UI + RTL) — BUILD MODE

CONTEXT:
G1 Infrastructure is COMPLETE and VERIFIED.
The system supports **30 locales** (including RTL).
NO automatic translation is allowed.
Localization must be COMPLETE, CONSISTENT, and TOTAL.

OBJECTIVE:
Implement **full static UI localization + RTL correctness** across the entire public frontend
for **ALL 30 SUPPORTED LOCALES**.

This phase does NOT touch dynamic DB content yet (destinations, guides, attractions bodies).
This phase is strictly about:
- UI strings
- Layout correctness
- RTL behavior
- SEO + Schema correctness

STRICT RULES:
- ❌ No automatic translation
- ❌ No AI-generated content
- ❌ No partial coverage
- ❌ No silent fallback to English
- ✅ Every visible string must be locale-aware
- ✅ RTL must be visually correct (not just dir="rtl")

SUPPORTED LOCALES:
All locales defined in the system (≈30), including:
ar, he, fa, ur (RTL)
and all LTR locales already configured.

SCOPE (ALL REQUIRED):

1) i18next STATIC STRINGS (30 LANGUAGES)
- Extract ALL hardcoded UI strings into translation keys
- Populate translation files for ALL 30 locales
- Affected areas:
  - PublicNav
  - PublicFooter
  - Homepage
  - Destinations / Attractions / Guides / News indexes
  - Search page
  - About / Contact
  - Privacy / Terms / Cookies / Security
  - Empty states, loaders, errors, buttons, labels

Acceptance:
- Grep for visible English literals returns ZERO results (excluding admin)
- Switching /ar, /fr, /de, /ja, etc. changes ALL UI text

2) GLOBAL COMPONENT WIRING
- Replace literals with t("...")
- Standardize namespaces:
  nav.*, footer.*, home.*, destinations.*, attractions.*, guides.*, news.*, common.*

Acceptance:
- Locale switch updates nav/footer immediately
- Mobile + desktop both correct

3) HOMEPAGE FULL LOCALIZATION
- Hero title / subtitle / badges
- Stats labels
- Category cards
- Experience sections
- FAQ questions & answers
- CTA buttons
- SEO meta text
- Schema.org FAQ text

Acceptance:
- /ar homepage fully Arabic
- /fr homepage fully French
- FAQ schema outputs localized text in page source

4) INDEX PAGES LOCALIZATION
Pages:
- Destinations
- Attractions
- Guides
- News
- Search

Zones:
- Hero headlines
- Filters
- Search placeholders
- Empty states
- FAQ sections
- SEO meta text

5) STATIC PAGES LOCALIZATION
- About
- Contact
- Privacy
- Terms
- Cookies
- Security

Acceptance:
- Full page content localized for ALL 30 locales
- No English remains in RTL pages

6) SCHEMA.ORG LOCALIZATION (CRITICAL)
Schema types:
- WebSite
- Organization
- FAQPage
- Article
- BreadcrumbList

Rules:
- inLanguage MUST match URL locale
- Text fields MUST use localized strings
- NO hardcoded English schema text

7) RTL LAYOUT FIXES (MANDATORY)
RTL locales: ar, he, fa, ur

Must fix:
- Carousels scroll direction
- Chevron / arrow mirroring
- Breadcrumb separators
- Sliders & progress bars
- text-align: start / end only
- padding/margin: logical props (ps / pe)

Acceptance:
- /ar, /he visually mirrored correctly
- Swipe direction intuitive
- No broken layouts

8) NUMBER & DATE FORMATTING
- Use Intl.NumberFormat(locale)
- Use Intl.DateTimeFormat(locale)
- Arabic numerals for RTL locales

Acceptance:
- Arabic digits (٠١٢٣٤٥٦٧٨٩) shown where applicable
- Dates formatted per locale

OUT OF SCOPE (DO NOT TOUCH):
- DB content translations
- Attractions bodies (Tiqets)
- Admin UI
- Partner dashboards
- Automatic translation systems

DELIVERABLE:
- 100% static UI localized for 30 locales
- RTL fully correct
- Schema localized
- ZERO hardcoded English in public UI
- Ready to proceed to G3 (Content + Media)

START BUILD.