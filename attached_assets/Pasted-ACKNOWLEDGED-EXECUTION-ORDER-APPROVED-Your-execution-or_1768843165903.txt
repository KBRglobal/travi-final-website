ACKNOWLEDGED — EXECUTION ORDER APPROVED

Your execution order is correct.
Proceed STRICTLY in the sequence below.
Each step must be completed and proven before moving to the next.

--------------------------------
EXECUTION CONTRACT (BINDING)
--------------------------------

STEP 1 — CREATE DESTINATION (ADMIN ONLY)
- Create "Test City" via Admin UI or Admin API
- NO manual SQL inserts
- Explicitly confirm:
  - slug = test-city
  - is_active = true

--------------------------------
STEP 2 — CODE PROOF (BLOCKERS REMOVED)
--------------------------------

Provide exact code snippets + file paths + line numbers proving:

A) public-api.ts
- NO ALLOWED_DESTINATION_IDS
- NO hardcoded ID filtering of any kind

B) destination-intelligence.ts
- NO SUPPORTED_DESTINATION_IDS
- NO purge / auto-delete logic tied to hardcoded destinations

This must be shown BEFORE any data proofs.

--------------------------------
STEP 3 — SQL PROOF (RAILWAY POSTGRESQL ONLY)
--------------------------------

Run against Railway PostgreSQL (RAILWAY_DATABASE_URL):

- Query showing test-city exists
- is_active = true
- Total destination count

Explicitly state:
“This query was executed against Railway PostgreSQL.”

--------------------------------
STEP 4 — API PROOF
--------------------------------

A) GET /api/public/destinations
- test-city included
- count matches DB

B) GET /api/public/destinations/test-city
- Status 200
- Payload includes hero, mood, SEO

--------------------------------
STEP 5 — RESTART PERSISTENCE PROOF (CRITICAL)
--------------------------------

- Restart server
- Re-run:
  - SQL query
  - Both API endpoints

Result must be identical.
No deletion, no filtering, no mutation.

--------------------------------
STEP 6 — FRONTEND PROOF
--------------------------------

- /destinations page:
  - Test City visible
- /destinations/test-city:
  - Page loads successfully
- Network tab:
  - Data comes from API
  - No static or fallback sources

--------------------------------
FINAL ACCEPTANCE RULE
--------------------------------

- No summaries
- No “works now” statements
- Proofs only, in order

Only after ALL steps pass:
- Create Destination blocker is CLOSED
- We resume remaining Destinations work (Media, OG, Schemas)