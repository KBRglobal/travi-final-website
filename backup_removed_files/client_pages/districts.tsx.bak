import ContentList from './content-list";

export default function Districts() {
  return <ContentList type="district" />;
}
npm run check 2>&1 | grep -c "error TS"
npm run check 2>&1 | grep "Property\|does not exist" | head -20
