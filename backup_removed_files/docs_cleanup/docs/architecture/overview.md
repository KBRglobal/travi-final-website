# 🏗️ Architecture Overview

> High-level architecture of Traviapp

---

## 📊 System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         CLIENTS                                  │
├─────────────────────────────────────────────────────────────────┤
│  Browser (React SPA)  │  Mobile  │  API Consumers  │  RSS Bots  │
└───────────────────────┴──────────┴─────────────────┴────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                     LOAD BALANCER / CDN                          │
│                      (Cloudflare)                                │
└─────────────────────────────────────────────────────────────────┘
                                │
                                ▼
┌─────────────────────────────────────────────────────────────────┐
│                    APPLICATION SERVER                            │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                    Express.js                            │   │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐  │   │
│  │  │   Routes    │  │ Middleware  │  │    Services     │  │   │
│  │  │  (187 APIs) │  │  Security   │  │  AI, Email, RSS │  │   │
│  │  └─────────────┘  └─────────────┘  └─────────────────┘  │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                   Static Files                           │   │
│  │                  (Vite Build)                            │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
           │                    │                    │
           ▼                    ▼                    ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│   PostgreSQL    │  │  Upstash Redis  │  │  Object Storage │
│   (Database)    │  │    (Cache)      │  │    (Media)      │
└─────────────────┘  └─────────────────┘  └─────────────────┘
           │
           ▼
┌─────────────────────────────────────────────────────────────────┐
│                    EXTERNAL SERVICES                             │
├─────────────────┬─────────────────┬─────────────────────────────┤
│     OpenAI      │      DeepL      │         Resend              │
│  (AI Content)   │  (Translation)  │        (Email)              │
└─────────────────┴─────────────────┴─────────────────────────────┘
```

---

## 🧩 Component Architecture

### Frontend (React SPA)

```
client/src/
├── pages/          # Route components (131 pages)
├── components/     # Reusable UI components
│   ├── ui/         # Base components (Radix UI)
│   └── article/    # Domain components
├── hooks/          # Custom React hooks
├── lib/            # Utilities
│   ├── api.ts      # API client
│   └── utils.ts    # Helpers
└── locales/        # i18n translations
```

### Backend (Express)

```
server/
├── index.ts        # Entry point
├── routes.ts       # Main router (8144 lines)
├── db.ts           # Database connection
├── auth.ts         # Authentication
├── security.ts     # Security middleware
├── ai-generator.ts # OpenAI integration
├── services/       # External services
└── *-routes.ts     # Feature routes
```

### Shared

```
shared/
├── schema.ts       # Drizzle ORM schema (45+ tables)
└── dubai-keywords.ts
```

---

## 🔄 Request Flow

```
1. Request arrives at Express server
           │
           ▼
2. Security middleware
   - Rate limiting
   - CORS
   - CSRF protection
           │
           ▼
3. Authentication
   - Session validation
   - Role checking
           │
           ▼
4. Route handler
   - Input validation (Zod)
   - Business logic
           │
           ▼
5. Database/External Services
   - Drizzle ORM queries
   - AI/Translation calls
           │
           ▼
6. Response
   - JSON formatting
   - Error handling
```

---

## 📦 Key Technologies

| Layer | Technology | Purpose |
|-------|------------|---------|
| **Frontend** | React 18 | UI framework |
| **Routing** | Wouter | Client routing |
| **Styling** | Tailwind CSS | CSS framework |
| **Components** | Radix UI | Accessible components |
| **State** | TanStack Query | Server state |
| **Backend** | Express | HTTP server |
| **ORM** | Drizzle | Database access |
| **Database** | PostgreSQL 16 | Primary storage |
| **Cache** | Upstash Redis | Performance cache |
| **AI** | OpenAI | Content generation |
| **Translation** | DeepL | Multi-language |
| **Email** | Resend | Transactional email |

---

## 🔐 Security Architecture

```
┌─────────────────────────────────────────┐
│             Security Layers              │
├─────────────────────────────────────────┤
│  1. Rate Limiting                        │
│     - Auth: 5/min                        │
│     - API: 100/min                       │
├─────────────────────────────────────────┤
│  2. Authentication                       │
│     - Session-based                      │
│     - 2FA/MFA (TOTP)                     │
│     - OAuth/OIDC                         │
├─────────────────────────────────────────┤
│  3. Authorization                        │
│     - RBAC (5 roles)                     │
│     - Permission checks                  │
├─────────────────────────────────────────┤
│  4. Input Validation                     │
│     - Zod schemas                        │
│     - SQL injection prevention           │
├─────────────────────────────────────────┤
│  5. Audit Logging                        │
│     - All actions tracked                │
│     - IP/User Agent logged               │
└─────────────────────────────────────────┘
```

---

## 📊 Data Architecture

### Content Types

```
contents (base)
    ├── attractions
    ├── hotels
    ├── dining
    ├── districts
    ├── events
    ├── itineraries
    ├── articles
    └── transports
```

### Relationships

```
contents ──┬── translations (1:many)
           ├── versions (1:many)
           ├── tags (many:many)
           ├── media (many:many)
           └── views (1:many)

users ─────┬── contents (1:many)
           ├── comments (1:many)
           └── audit_logs (1:many)
```

---

## 🌐 Multi-Language Architecture

```
┌─────────────────────────────────────────┐
│           Translation Flow               │
├─────────────────────────────────────────┤
│  1. Content created (English)            │
│              │                           │
│              ▼                           │
│  2. Translation requested                │
│              │                           │
│              ▼                           │
│  3. DeepL API (11 languages)             │
│     OR Claude Haiku (6 languages)        │
│              │                           │
│              ▼                           │
│  4. Stored in translations table         │
│              │                           │
│              ▼                           │
│  5. Served via /api/public/content/:slug/:locale
└─────────────────────────────────────────┘
```

---

## 📈 Scalability Considerations

### Current Architecture
- Single server deployment
- PostgreSQL for all data
- Redis for caching

### Scaling Options
- Horizontal: Multiple app servers behind load balancer
- Database: Read replicas, connection pooling
- Cache: Distributed Redis cluster
- Storage: CDN for media files
- AI: Queue-based processing for heavy tasks
