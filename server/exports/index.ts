// Stub - Export Center module disabled
import { Router, Express } from "express";

export const exportsRoutes = Router();
export const exportCenterRoutes = Router();

export function registerExportCenterRoutes(_app: Express): void {
  // Disabled
}
