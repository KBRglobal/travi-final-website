/**
 * Admin Ingestion Routes (Stub)
 * Data ingestion functionality was removed during codebase cleanup.
 */

import type { Express } from 'express';

export function registerAdminIngestionRoutes(app: Express): void {
  // Stub - ingestion routes disabled
}
